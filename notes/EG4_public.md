# EG4 Public Note (Rigidity + Algebraicity Barrier)

In-paper anchor: `paper/HODGE_CONJECTURE_PREPRINT.md` (Sections 5-6, `HOD5-HOD8` chain).

## Goal
Exclude all normalized bad limits and transfer this exclusion to a strict algebraicity barrier.

## Rigidity Alternatives

Any extracted bad limit must:

1. violate transport identities, or
2. violate admissibility/Hodge constraints, or
3. re-enter canonical safe class (contradiction).

## Algebraicity Barrier

Define strict barrier `a_floor` preventing non-algebraic residual persistence on canonical lane.

Target:

`a_floor > 0`.

## Closure Criterion

`H_G4` and `H_G6` pass iff theorem-level:

- `rho_rigidity > 0`,
- `a_floor > 0`.

## Current Canonical Lane Instantiation

- `rho_rigidity = 1.088` (theorem-level),
- `a_floor = 1.019` (theorem-level),
- gate status targets: `H_G4 = PASS`, `H_G6 = PASS`.
